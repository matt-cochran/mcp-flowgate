schema: flowgate.repo/v1
name: swe-core
namespace: swe
version: 0.6.0
description: |
  Test fixture — SPEC §9 multi-repo loading.
  Single capability + single orchestrator that references it via an
  unprefixed name (rewriting target).
